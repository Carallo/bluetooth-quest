{
  "name": "bluetooth-server",
  "version": "1.0.0",
  "description": "A custom bluetooth server plugin",
  "main": "dist/plugin.js",
  "module": "dist/esm/index.js",
  "types": "dist/plugin.d.ts",
  "scripts": {
    "build": "npm run clean && tsc && rollup -c rollup.config.js",
    "clean": "rimraf dist"
  },
  "keywords": [
    "capacitor",
    "plugin",
    "native"
  ],
  "author": "Jules",
  "license": "MIT",
  "capacitor": {
    "android": {
      "src": "android"
    }
  },
  "peerDependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.32.0",
    "typescript": "^4.0.3"
  }
}
